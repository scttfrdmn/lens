# AWS IDE Environment Variables
# Copy this file to .env and update with your values

# AWS Profile
# The AWS CLI profile to use for authentication
# Default: aws
AWS_PROFILE=aws

# LocalStack Configuration (for integration tests)
# Endpoint URL for LocalStack (when running integration tests)
# Default: http://localhost:4566
AWS_ENDPOINT_URL=http://localhost:4566

# LocalStack Credentials (for integration tests)
# These are dummy credentials used by LocalStack
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# Default AWS Region
# The AWS region to use for all operations
# Default: us-west-2
AWS_DEFAULT_REGION=us-west-2
