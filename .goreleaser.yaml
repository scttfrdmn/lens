version: 2

project_name: aws-ide

before:
  hooks:
    - go work sync
    - go mod tidy -C apps/jupyter
    - go mod tidy -C apps/rstudio
    - go mod tidy -C pkg

builds:
  - id: aws-jupyter
    main: ./apps/jupyter/cmd/aws-jupyter
    binary: aws-jupyter
    dir: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

  - id: aws-rstudio
    main: ./apps/rstudio/cmd/aws-rstudio
    binary: aws-rstudio
    dir: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

  - id: aws-vscode
    main: ./apps/vscode/cmd/aws-vscode
    binary: aws-vscode
    dir: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: jupyter
    builds:
      - aws-jupyter
    name_template: >-
      aws-jupyter_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md
      - apps/jupyter/README.md
      - apps/jupyter/environments/*.yaml
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

  - id: rstudio
    builds:
      - aws-rstudio
    name_template: >-
      aws-rstudio_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md
      - apps/rstudio/README.md
      - apps/rstudio/environments/*.yaml
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

  - id: vscode
    builds:
      - aws-vscode
    name_template: >-
      aws-vscode_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md
      - apps/vscode/README.md
      - apps/vscode/environments/*.yaml
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - '^chore:'
      - '^build:'
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: 'Bug fixes'
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999

release:
  github:
    owner: scttfrdmn
    name: aws-ide
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## Release {{ .Tag }} ({{ .Date }})

    AWS IDE is a monorepo containing multiple cloud-based IDE launchers:
    - **aws-jupyter**: Launch Jupyter Lab instances on AWS EC2 (Production)
    - **aws-rstudio**: Launch RStudio Server instances on AWS EC2 (Production)
    - **aws-vscode**: Launch VSCode Server instances on AWS EC2 (Beta)

    All tools share infrastructure from the `pkg/` module for consistent behavior.
  footer: |
    ## Thanks!

    Those were the changes on {{ .Tag }}!

    See the [CHANGELOG](https://github.com/scttfrdmn/aws-ide/blob/{{ .Tag }}/CHANGELOG.md) for detailed release notes.

# Homebrew formulas for both tools
# Note: 'brews' is the correct field name for Homebrew tap formulas
brews:
  - name: aws-jupyter
    ids:
      - jupyter
    repository:
      owner: scttfrdmn
      name: homebrew-tap
    folder: Formula
    homepage: https://github.com/scttfrdmn/aws-ide
    description: CLI tool for launching Jupyter Lab instances on AWS EC2 Graviton processors
    license: Apache-2.0
    install: |
      bin.install "aws-jupyter"

      # Install environment templates
      pkgshare.install Dir["apps/jupyter/environments/*.yaml"]
      (pkgshare/"environments").mkpath
      Dir["apps/jupyter/environments/*.yaml"].each do |env|
        (pkgshare/"environments").install env
      end
    test: |
      system "#{bin}/aws-jupyter", "--version"

  - name: aws-rstudio
    ids:
      - rstudio
    repository:
      owner: scttfrdmn
      name: homebrew-tap
    folder: Formula
    homepage: https://github.com/scttfrdmn/aws-ide
    description: CLI tool for launching RStudio Server instances on AWS EC2 Graviton processors
    license: Apache-2.0
    install: |
      bin.install "aws-rstudio"

      # Install environment templates
      pkgshare.install Dir["apps/rstudio/environments/*.yaml"]
      (pkgshare/"environments").mkpath
      Dir["apps/rstudio/environments/*.yaml"].each do |env|
        (pkgshare/"environments").install env
      end
    test: |
      system "#{bin}/aws-rstudio", "--version"

  - name: aws-vscode
    ids:
      - vscode
    repository:
      owner: scttfrdmn
      name: homebrew-tap
    folder: Formula
    homepage: https://github.com/scttfrdmn/aws-ide
    description: CLI tool for launching VSCode Server instances on AWS EC2 Graviton processors (Beta)
    license: Apache-2.0
    install: |
      bin.install "aws-vscode"

      # Install environment templates
      pkgshare.install Dir["apps/vscode/environments/*.yaml"]
      (pkgshare/"environments").mkpath
      Dir["apps/vscode/environments/*.yaml"].each do |env|
        (pkgshare/"environments").install env
      end
    test: |
      system "#{bin}/aws-vscode", "--version"

# Scoop manifests for Windows
scoops:
  - name: aws-jupyter
    ids:
      - jupyter
    repository:
      owner: scttfrdmn
      name: scoop-bucket
    homepage: https://github.com/scttfrdmn/aws-ide
    description: CLI tool for launching Jupyter Lab instances on AWS EC2 Graviton processors
    license: Apache-2.0

  - name: aws-rstudio
    ids:
      - rstudio
    repository:
      owner: scttfrdmn
      name: scoop-bucket
    homepage: https://github.com/scttfrdmn/aws-ide
    description: CLI tool for launching RStudio Server instances on AWS EC2 Graviton processors
    license: Apache-2.0

  - name: aws-vscode
    ids:
      - vscode
    repository:
      owner: scttfrdmn
      name: scoop-bucket
    homepage: https://github.com/scttfrdmn/aws-ide
    description: CLI tool for launching VSCode Server instances on AWS EC2 Graviton processors (Beta)
    license: Apache-2.0

# Linux packages (deb and rpm)
nfpms:
  - id: jupyter-packages
    builds:
      - aws-jupyter
    package_name: aws-jupyter
    vendor: Scott Friedman
    homepage: https://github.com/scttfrdmn/aws-ide
    maintainer: Scott Friedman <scttfrdmn@users.noreply.github.com>
    description: CLI tool for launching Jupyter Lab instances on AWS EC2 Graviton processors
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/bin
    contents:
      - src: apps/jupyter/environments/*.yaml
        dst: /usr/share/aws-jupyter/environments/

  - id: rstudio-packages
    builds:
      - aws-rstudio
    package_name: aws-rstudio
    vendor: Scott Friedman
    homepage: https://github.com/scttfrdmn/aws-ide
    maintainer: Scott Friedman <scttfrdmn@users.noreply.github.com>
    description: CLI tool for launching RStudio Server instances on AWS EC2 Graviton processors
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/bin
    contents:
      - src: apps/rstudio/environments/*.yaml
        dst: /usr/share/aws-rstudio/environments/

  - id: vscode-packages
    builds:
      - aws-vscode
    package_name: aws-vscode
    vendor: Scott Friedman
    homepage: https://github.com/scttfrdmn/aws-ide
    maintainer: Scott Friedman <scttfrdmn@users.noreply.github.com>
    description: CLI tool for launching VSCode Server instances on AWS EC2 Graviton processors (Beta)
    license: Apache-2.0
    formats:
      - deb
      - rpm
    bindir: /usr/bin
    contents:
      - src: apps/vscode/environments/*.yaml
        dst: /usr/share/aws-vscode/environments/
