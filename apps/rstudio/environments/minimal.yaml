name: minimal
ami_base: ubuntu-24.04-arm64
instance_type: t4g.small
ebs_volume_size: 20

packages:
  - build-essential
  - git
  - curl
  - wget
  - vim

r_packages:
  - rmarkdown

environment_vars:
  RSTUDIO_WHICH_R: /usr/bin/R
  R_LIBS_USER: ~/R/library
